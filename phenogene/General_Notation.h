#ifndef GENERAL_NOTATION_H
#define GENERAL_NOTATION_H

#include <iostream>
#include <sstream>
#include <fstream>
#include <stdio.h>
#include <stdlib.h>
#include <utility>
#include <vector>
#include <cmath>
#include <map>
#include <cstring>
#include <string>
using namespace std;

#define fori(a, b) for (int i = (a); i < (b); i++)
#define forj(a, b) for (int j = (a); j < (b); j++)
#define fork(a, b) for (int k = (a); k < (b); k++)
#define max_dataset_size 10000
#define input_l 100
#define output_l 100
#define hidden_l 100
#define hidden_layer 0
#define output_layer 1
#define segmoidal 1
#define max_iterations 1000

#endif // GENERAL_NOTATION_H
